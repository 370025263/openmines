[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openmines_gym"]

[project]
name = "openmines_gym"
version = "0.0.2"
dependencies = [
  "gymnasium",
  "pygame>=2.1.3",
  "pre-commit",
]
